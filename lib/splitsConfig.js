// Client-safe copy of splits.config.js — no Node.js dependencies.
// Mirror any changes here AND in splits.config.js.
// { ticker, date (YYYY-MM-DD), ratio } — ratio = new shares per old share.

export const SPLITS = [
  { ticker: 'NFLX', date: '2026-01-01', ratio: 10 },
];
